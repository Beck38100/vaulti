# Fins de ligne normalisées dans le dépôt (LF), quel que soit le poste.
# Indispensable le jour où le projet sera compilé depuis un Mac pour iOS.
* text=auto eol=lf

# Fichiers binaires : jamais de conversion.
*.png binary
*.jpg binary
*.ico binary
*.ttf binary
*.otf binary
*.keystore binary
*.jks binary
*.p12 binary
